# PlanDetailPage UI/UX 개선안 — 검토 요청

## 현재 문제점

### 1. 상단 네비게이션 과밀
- "일정 / 메모 / 앨범" 탭 + "목록 / 타임라인 / 일별 / 📅" 뷰 + "일정 추가" 버튼
- 너무 많아서 지저분함

### 2. 지도 중복
- 상단 "여행 동선" collapse 지도가 항상 전체 핀 표시
- DayView에도 별도 지도 있음 → 중복
- **해결:** DayView 지도 제거, 상단 지도가 viewMode='daily'일 때 해당 날짜 핀만 필터

### 3. 목록(vertical) 뷰 개선
- 현재: 전체 일정 쭉 나열, 날짜 구분 없음
- **해결:** 날짜별 구분자 추가 (Day 1 · 3/15(토) 헤더)

### 4. 타임라인(horizontal) 뷰 개선
- ScheduleCard가 너무 큼 → 한 화면에 적게 보임
- **해결:** 타임라인에서는 compact 카드 사용 (폰트/패딩 축소)

### 5. DayView 인디케이터 (도트)
- 배경색과 너무 비슷해서 안 보임
- **해결:** 대비 높이기 (더 진한 색 + 약간 큰 크기, 또는 pill 형태로 "D1 D2 D3...")

### 6. 일정 추가 폼
- AI 텍스트 입력이 한 줄 input → textarea로 변경 (여러 줄 자연어)
- 전체적으로 개발 덜된 느낌 → 폼 디자인 정리

## 구현 항목 상세

### A. 상단 레이아웃 정리
**Before:**
```
[일정] [메모] [앨범]    ← tabs-boxed

일정                    [목록|타임라인|일별|📅]  [일정 추가]
```

**After:**
```
[일정] [메모] [앨범]    ← 메인 탭 유지

[목록|타임라인|일별|캘린더]              [+ 추가]
```
- "일정" 텍스트 제거 (메인 탭이 이미 "일정"이니까)
- 뷰모드 탭을 왼쪽, 추가 버튼을 오른쪽 (아이콘만, compact)

### B. 상단 지도 + DayView 연동
- `viewMode === 'daily'` 일 때 상단 TravelMap에 `currentDate` 전달
- 해당 날짜 일정 핀만 표시
- DayView 내부 지도 제거

### C. 목록 뷰에 날짜 구분자
```tsx
{Object.entries(groupedSchedules).map(([date, schedulesForDate]) => (
  <div key={date}>
    <div className="날짜 헤더">Day {n} · {formatDisplayDate(date)}</div>
    {schedulesForDate.map(schedule => <ScheduleCard ... />)}
  </div>
))}
```

### D. 타임라인 compact 카드
- card-body 패딩 줄이기 (p-4 → p-2.5)
- 제목 text-xl → text-base
- 시간 badge-lg → badge-md
- 메모 영역 줄이기 (line-clamp-2)
- 컬럼 폭 320px → 280px

### E. DayView 인디케이터
- 도트 → pill 형태: `D1` `D2` `D3` ...
- 선택된 날: primary 색 + bold
- 미선택: ghost 스타일
- 일정 없는 날: opacity 낮게

### F. 일정 추가 폼
- AI 텍스트 입력: `<input>` → `<textarea rows={3}>`
- 전체 폼 간격/정렬 정리

## 파일 목록
- `src/pages/PlanDetailPage.tsx` — 메인 (레이아웃, 탭, 뷰모드, 폼)
- `src/components/DayView.tsx` — 일별 뷰 (지도 제거, 인디케이터 개선)
- `src/components/ScheduleCard.tsx` — 카드 (compact 모드 prop 추가)
- `src/components/TravelMap.tsx` — 지도 (변경 없을 수 있음)
